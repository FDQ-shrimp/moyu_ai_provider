# qwen3.6-plus 流式续测结果

日期：2026-09-21（报告时间字段使用 UTC）。状态：授权续测完成，进程退出；本轮累计
10 次请求已到上限，不再发送请求。没有模型查询、凭据校验、自动重试或重定向。

## 1. 修改范围

本次只修改 scripts/verify_fc_live.py、tests/test_fc_live_runner.py 及相关开发记录。
插件业务适配器、离线保护、manifest、生产 requirements 的 SHA256 与本次开始前相同。
没有修改模型 YAML/能力声明、版本、依赖或其他模块；没有提交、推送、打包、发布。

验证器修正：

- 独立累计上游 content，reasoning_content 只用于核对 SDK 0.9.0 的已知输出包装。
  不删除任意文本、不搜索任意位置的 5。最终 content 去除首尾空白后必须严格等于 5。
- 同时逐段构造已知 SDK 包装的预期文本，与真实 SDK 解析输出作完整比较。
  同一分片同时带非空思考和答案可能被该 SDK 丢失答案，此形态拒绝通过，记为有歧义。
- 在验收断言之前、以及 SDK 解析异常的 finally 路径保存安全诊断：最终答案是否存在/
  匹配、是否有思考字段、对应关系是否明确、解析是否完成、终止原因、usage 和响应哈希。
  不保存原始思考/回答、Key、认证头或任意异常文本。
- usage 缺失或非有效整数时为 null（未知），不把 SDK 推算用量冒充上游实际用量。
  响应哈希针对已观察到的 JSON 数据，解析中途失败时只代表已观察部分，不冒称完整响应。
- 新增 --resume-stream：校验既有报告哈希/6 次历史/非流式通过状态；新文件只记录续测，
  每次发送前核对父报告未变，以 6 + 新次数累计计数。新文件已存在则拒绝重跑。

## 2. 离线验证与发布预检

先增加用例，确认原严格比较规则不能通过“思考+正确答案”，随后修正验证器。
新增测试覆盖：思考+正确答案、思考中含 5 但最终答案错误、无最终答案、截断、
无思考正常答案、思考和答案跨分片、字面 think 标签不能绕过检查、同片混合有歧义、
SDK 输出不匹配、SDK 解析失败仍保存诊断、未知用量、续测第 7～10 次计数、原记录保留、
哈希变化禁止发送以及国内不确定时不发送海外请求。

一个初始测试假定缺失 usage 会抛 KeyError，实际 SDK 尝试下载 tokenizer，已被网络
保护阻断，未放行下载。测试改用明确畸形的 usage 触发真实 SDK 类型错误来验证异常诊断，
另独立检查缺失 usage 保持未知；未跳过测试或弱化网络保护。

真实续测前及结束后均运行：

- 完整离线回归：225 passed、0 failed、0 skipped，22 条 SDK Pydantic .dict() 弃用警告。
- 发布预检：143 passed、0 failed、0 warnings。
- git diff --check：通过（Git 的 LF/CRLF 提示不是测试失败）。

环境保持 Python 3.12.14 / dify_plugin 0.9.0，解释器：
C:/Users/fangd/Workspace/CodeX/Dify/.venvs/moyu-fc-sdk090/Scripts/python.exe。
真实调用仍走当前 MoyuLargeLanguageModel → 真实 SDK → 指定站点。

## 3. 本次真实结果

| 站点 | 精确模型 | 流式闭环 | 新请求编号 | 工具分片 | 最终答案及 SDK 对应关系 | 原因 |
| --- | --- | --- | --- | --- | --- | --- |
| 国内 | qwen3.6-plus | 通过 | 7、8 | 7 个，正确恢复 | 上游 content 独立匹配 5；SDK 包装后完整匹配；finish=stop | 无失败 |
| 海外 | qwen3.6-plus | 通过 | 9、10 | 6 个，正确恢复 | 上游 content 独立匹配 5；SDK 包装后完整匹配；finish=stop | 无失败 |

两站工具 ID/函数 add/参数 a=2,b=3 与上游一致；实际执行本地加法；以相同 tool_call_id
回传结果；第二轮不带新工具定义。两站最终回答均含独立 reasoning_content 字段，
但最终 content 本身正确，SDK 输出完整对应，不靠删除思考或 HTTP 200 判通过。

本次新增 4 次；此前 6 次；累计 10 次（国内共 6 次、海外共 4 次）。
未重新调用已通过的非流式闭环。

## 4. 历史与新证据不可混同

原文件 scripts/fc_phase2_round1_report.json 保留原字节、原 6 次记录和第 6 次未证实状态。
原 SHA256（续测前后相同）：
57F8CE5032733A7EA13B9ECFFB383B3697CE04B61AC725CE1F98C2A0B7559D72。

新文件 scripts/fc_phase2_stream_resume_report.json 保存 prior_attempts=6、
parent_report_sha256、累计次数及第 7～10 次证据。
新 SHA256：
0DFDEA601F90AAFE93ED1CBCD33BCA4991CECBAB8326FD581CC6BDA23727B43D。

这证明本次新响应的闭环通过，不是恢复了第 6 次缺失的响应，也不是追溯改写旧结果。

## 5. 费用与仍未确认事项

预算仍是整个本轮国内 CNY 10、海外 USD 2，没有新增一份预算。
单价、实际扣费和平台硬额度未核实，费用估算保持未知。

| 新请求 | 上游 prompt_tokens | completion_tokens | total_tokens |
| --- | --- | --- | --- |
| 7 国内 | 314 | 264 | 578 |
| 8 国内 | 328 | 57 | 385 |
| 9 海外 | 314 | 129 | 443 |
| 10 海外 | 193 | 40 | 233 |

本次上游报告共 1639 tokens。加上原报告已有的 5 次 usage，已记录合计 3488 tokens；
第 6 次 usage 未保存，仍未知，不补造全轮总量或费用。

第 7 次请求发送的 max_tokens=256，但上游报告 completion_tokens=264、finish=tool_calls。
统计口径或上游对参数的处理尚未核实；不能宣称输出 token 参数或本地次数限制等于账单硬限制。
本次没有为此追加查询或请求。

本轮没有流式闭环方面的剩余失败，但结论只适用于所测站点、精确模型和 SDK 版本的
单工具样例；没有验证多工具、其他模型、其他 SDK 或 Dify Agent UI 端到端。
正式模型能力标签仍未开启，安装包未生成。后续变更须单独确认，本轮到此停止。
