# === SECRETS — NEVER COMMIT ===
# Real environment file with API keys / debug install key
.env
.env.*
!.env.example

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# === IDE / OS ===
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# === Dify build artefacts ===
# The produced .difypkg is uploaded separately to the dify-plugins repo,
# no need to keep it in source control.
*.difypkg

# === Local scratch / reports that shouldn't be tracked ===
scripts/preflight_report.json
scripts/probe_report.json
